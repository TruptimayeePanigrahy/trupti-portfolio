'use client';

// This component is no longer needed as theme toggle is now in Navigation
// Keeping it for backward compatibility but it won't be used
export default function ThemeToggle() {
    return null;
}
